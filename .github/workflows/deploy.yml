- name: Commit & Push
  run: |
    echo "Current directory:"
    pwd

    echo "Root files:"
    ls -la

    echo "Space files:"
    ls -la space

    cd space

    echo "After cd:"
    pwd

    git remote -v